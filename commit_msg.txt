feat: Phase 3 — Dashboard remembers & shows prior analytics per upload

- Added get_run_summaries_by_upload(engagement_id) store helper: returns {upload_id: {count, last_status, last_run_id, last_finished}}
- Updated uploads.py::dashboard route to fetch run_summaries and pass to template
- Added Analytics column to index.html dashboard uploads table: shows "{count} run(s)" + last_status badge (links to upload detail), or "—" if no runs

Co-Authored-By: Claude Haiku 4.5 <noreply@anthropic.com>
